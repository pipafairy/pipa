# Pietra Costantin Portfolio

Static portfolio draft generated from the current Claude Design handoff. The latest imported source was `24Pietra Costantin v8.dc.html`.

## Publishing

This folder can be published as a static site on GitHub + Cloudflare Pages.

- Build command: none
- Output directory: `/`
- Entry file: `index.html`

Keep designing by replacing `index.html` and any files in `uploads/` as the design evolves.

## Fonts

SF Pro font files are available in `assets/fonts/sf-pro/` for future typography work.

## Pending Assets

The latest HTML references `uploads/pietra-profile.jpg` and `uploads/asfalto.jpg`. Add those files to `uploads/` when the final images are ready.
